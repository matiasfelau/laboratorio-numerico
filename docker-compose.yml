services:
  simulador:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: simulador-modelado
    ports:
      - "5000:5000"
    environment:
      APP_HOST: "0.0.0.0"
      APP_PORT: "5000"
      APP_DEBUG: "1"
    restart: unless-stopped
